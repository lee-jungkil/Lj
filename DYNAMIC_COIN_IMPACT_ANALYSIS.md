# 동적 코인 갱신 영향 범위 분석 리포트

**작성일**: 2026-02-13  
**분석 대상**: `self.tickers` 변수의 영향 범위  
**결론**: ⚠️ **동적 코인 갱신은 독립적이지 않으며, 전체 시스템에 즉시 영향을 미칩니다!**

---

## 🚨 중요 발견: 동적 코인 갱신은 독립적이지 않음!

### **문제점**:
동적 코인 갱신(3분마다)이 실행되면 `self.tickers` 변수가 즉시 변경되며, 이는 **모든 실행 주기에 즉시 영향을 미칩니다**.

```python
# Line 1815-1822: 동적 코인 갱신
if self.dynamic_coin_selector and self.dynamic_coin_selector.should_update():
    old_count = len(self.tickers)
    self.tickers = self.dynamic_coin_selector.get_coins(method=Config.COIN_SELECTION_METHOD)
    # ⚠️ 이 순간 self.tickers가 변경됨 → 모든 로직에 즉시 영향!
```

---

## 📊 `self.tickers` 사용 위치 분석

### **1. 초기화 (1회)**

| 라인 | 코드 | 설명 |
|------|------|------|
| 280 | `self.tickers = self.dynamic_coin_selector.get_coins()` | 동적 선정 시 초기 코인 목록 |
| 286 | `self.tickers = self.api.validate_tickers(all_tickers)` | 화이트리스트 모드 초기화 |

---

### **2. 동적 갱신 (3분마다)** ⚠️ **영향 발생 지점**

| 라인 | 코드 | 영향 범위 |
|------|------|----------|
| **1820** | `self.tickers = self.dynamic_coin_selector.get_coins()` | **전체 시스템에 즉시 영향** |

**갱신 시점**:
- 3분(180초)마다 실행
- `should_update()` 조건 충족 시 자동 갱신
- 갱신 즉시 `self.tickers` 리스트 교체

---

### **3. 전체 코인 스캔 (20초마다)** ⭐ **영향받음**

| 라인 | 코드 | 영향 |
|------|------|------|
| **1840** | `self.orderbook_monitor.monitor_orderbook(self.tickers[:20])` | 호가창 모니터링 대상 변경 |
| **1844** | `total_tickers = len(self.tickers)` | 배치 처리 개수 변경 |
| **1848** | `batch_tickers = self.tickers[batch_start:batch_end]` | **스캔 대상 코인 즉시 변경** |
| **1864-1866** | `for ticker in batch_tickers: analyze_ticker(ticker, strategy)` | **매수 신호 생성 대상 변경** |

**영향 내용**:
- ✅ **긍정적**: 거래량/RSI 기준으로 더 좋은 코인으로 교체
- ⚠️ **부정적**: 스캔 중간에 코인 목록 변경 시 일부 코인 누락 가능
- ⚠️ **부정적**: 이전 코인의 데이터 축적 손실 (차트 분석 연속성 끊김)

**예시 시나리오**:
```
08:00:00 - 전체 스캔 시작 (35개 코인: BTC, ETH, XRP, ...)
08:00:10 - 배치 #1 완료 (BTC, ETH, XRP, ADA, SOL 분석)
08:00:15 - 🔄 동적 갱신 실행! (self.tickers 변경: BTC, ETH, DOGE, AVAX, ...)
08:00:20 - 배치 #2 시작 (이제 DOGE, AVAX, ... 분석) ← XRP 누락 가능!
```

---

### **4. 급등/급락 감지 (5초마다)** ⭐ **영향받음**

| 라인 | 코드 | 영향 |
|------|------|------|
| **1400** | `prices_dict = self.api.get_current_prices(self.tickers)` | **급등 감지 대상 코인 변경** |
| **1406** | `for ticker in self.tickers: ...` | **초단타 진입 대상 변경** |

**영향 내용**:
- ✅ **긍정적**: 급등 가능성 높은 코인으로 교체 (거래량/변동성 기준)
- ⚠️ **부정적**: 이전에 모니터링하던 코인의 급등 패턴 추적 끊김
- ⚠️ **부정적**: 갱신 직후 새 코인의 급등 이력 없음 (최소 5초 대기)

**예시 시나리오**:
```
08:00:00 - XRP 급등 감지 시작 (점수: 65/100)
08:00:05 - XRP 급등 점수 상승 (점수: 72/100)
08:00:10 - XRP 급등 점수 상승 (점수: 78/100) ← 진입 임박!
08:00:12 - 🔄 동적 갱신! XRP → DOGE 교체
08:00:15 - XRP 급등 추적 중단 (목록에서 제외됨) ← 진입 기회 손실!
```

---

### **5. 화면 표시** ⭐ **영향받음**

| 라인 | 코드 | 영향 |
|------|------|------|
| **1771** | `self.display.update_scan_status(f"코인 {len(self.tickers)}개 ...")` | 모니터링 개수 표시 변경 |
| **2187** | `self.display.update_bot_status(f"{len(self.tickers)}개 모니터링")` | 상태 표시 변경 |

**영향 내용**:
- ✅ **즉시 반영**: 사용자에게 코인 목록 변경 알림
- ✅ **투명성**: 갱신 시점 명확히 표시

---

## 🔄 동적 갱신의 실제 영향 범위

### **영향받는 주기**:

| 주기 | 간격 | 영향 | 영향 정도 |
|------|------|------|----------|
| **전체 스캔** | 20초 | `self.tickers` 기준 배치 분석 | ⚠️ **높음** |
| **급등 감지** | 5초 | `self.tickers` 기준 급등 탐지 | ⚠️ **높음** |
| **포지션 체크** | 7초 | 영향 없음 (보유 포지션 기준) | ✅ **없음** |
| **화면 갱신** | 3초 | 코인 개수 표시 변경 | ✅ **미미함** |

### **영향받지 않는 부분**:

✅ **일반 포지션 체크**: `self.risk_manager.positions` 기준으로 작동하므로 `self.tickers`와 무관  
✅ **보유 포지션 관리**: 이미 매수한 코인은 목록에서 제외되어도 계속 추적  
✅ **청산 로직**: 보유 중인 코인의 10가지 청산 조건은 계속 작동

---

## ⚠️ 잠재적 문제점

### **1. 스캔 중간 코인 목록 변경**

**시나리오**:
```python
# 전체 스캔 시작 (20초 주기)
for batch in batches:  # 35개 코인을 5개씩 배치 처리
    for ticker in batch:
        analyze_ticker(ticker, strategy)  # 매수 신호 분석
    # ⚠️ 이 시점에 동적 갱신 발생 가능!
    # → self.tickers 변경 → 다음 배치의 코인 목록 바뀜
```

**문제**:
- 일부 코인이 스캔에서 누락될 수 있음
- 새로 추가된 코인은 다음 스캔까지 대기

**해결 방법**:
```python
# 스캔 시작 시 코인 목록 고정
def scan_all_coins(self):
    tickers_snapshot = self.tickers.copy()  # 스냅샷
    for ticker in tickers_snapshot:
        analyze_ticker(ticker, strategy)
```

---

### **2. 급등 추적 연속성 끊김**

**시나리오**:
```
08:00:00 - XRP 급등 감지 시작 (점수: 65/100)
08:00:05 - XRP 급등 점수 상승 (점수: 72/100)
08:00:12 - 🔄 동적 갱신! XRP 제외 → 급등 추적 중단
```

**문제**:
- 급등 진입 임박 코인이 목록에서 제외되면 진입 기회 손실
- 새 코인은 이력이 없어 최소 5초 이상 대기 필요

**해결 방법**:
```python
# 급등 추적 중인 코인은 목록에서 제외하지 않음
def update_tickers_safely(self):
    new_tickers = self.dynamic_coin_selector.get_coins()
    
    # 급등 추적 중인 코인 보존
    tracking_tickers = [t for t in self.tickers 
                        if self.surge_detector.is_tracking(t)]
    
    # 병합
    self.tickers = list(set(new_tickers + tracking_tickers))
```

---

### **3. 데이터 축적 손실**

**시나리오**:
```
- XRP를 3분간 모니터링 (OHLCV 데이터 축적)
- 🔄 동적 갱신: XRP 제외
- 다음 갱신에서 XRP 재진입
- → 이전 데이터 축적 손실 (차트 분석 연속성 끊김)
```

**문제**:
- RSI, MACD 등 차트 지표의 연속성 끊김
- 단기 패턴 분석 정확도 저하

**해결 방법**:
```python
# OHLCV 데이터 캐싱 (30분간 보존)
self.ohlcv_cache[ticker] = {
    'data': df,
    'timestamp': time.time()
}
```

---

## ✅ 현재 동작 방식 (v6.30.8)

### **동적 갱신 흐름**:
```
1. 초기화 (1회)
   └─ self.tickers = get_coins() (35개 선정)

2. 실행 루프
   ├─ 20초마다: 전체 스캔 (self.tickers 기준)
   ├─ 5초마다: 급등 감지 (self.tickers 기준)
   ├─ 7초마다: 포지션 체크 (positions 기준, 독립)
   └─ 3분마다: 🔄 self.tickers 갱신
       ├─ should_update() 체크
       ├─ get_coins() 실행 (거래량/RSI/변동성 재분석)
       └─ self.tickers = new_coins (즉시 교체)
            ↓
            ⚠️ 다음 스캔/급등 감지부터 새 목록 사용
```

---

## 📋 권장 개선 사항

### **1. 스캔 중간 갱신 방지** (필수)

**현재 문제**:
```python
# Line 1824-1893: 전체 스캔
for batch in batches:
    for ticker in self.tickers:  # ⚠️ 중간에 변경 가능!
        analyze_ticker(ticker, strategy)
```

**개선 방법**:
```python
# 스캔 시작 시 스냅샷
def full_scan(self):
    tickers_snapshot = self.tickers.copy()  # 고정
    for ticker in tickers_snapshot:
        analyze_ticker(ticker, strategy)
```

---

### **2. 급등 추적 연속성 보장** (권장)

**현재 문제**:
```python
# 급등 추적 중인 코인이 목록에서 제외될 수 있음
self.tickers = new_coins  # 즉시 교체
```

**개선 방법**:
```python
def update_tickers_with_tracking(self, new_tickers):
    # 급등 점수 70 이상인 코인 보존
    tracking = [t for t in self.tickers 
                if self.surge_detector.get_score(t) >= 70]
    
    # 병합 (중복 제거)
    self.tickers = list(set(new_tickers + tracking))[:40]  # 최대 40개
```

---

### **3. OHLCV 캐싱 추가** (선택)

**현재 문제**:
```python
# 매번 API 호출
df = self.api.get_ohlcv(ticker, interval="minute5", count=200)
```

**개선 방법**:
```python
def get_cached_ohlcv(self, ticker, interval="minute5", count=200):
    cache_key = f"{ticker}_{interval}"
    now = time.time()
    
    # 30초 이내 캐시 재사용
    if cache_key in self.ohlcv_cache:
        cached_data, cached_time = self.ohlcv_cache[cache_key]
        if now - cached_time < 30:
            return cached_data
    
    # API 호출
    df = self.api.get_ohlcv(ticker, interval, count)
    self.ohlcv_cache[cache_key] = (df, now)
    return df
```

---

## 🎯 최종 결론

### **동적 코인 갱신은 독립적이지 않습니다!**

| 항목 | 상태 | 설명 |
|------|------|------|
| **독립성** | ❌ **아니요** | `self.tickers` 변경 시 모든 로직 즉시 영향받음 |
| **전체 스캔 영향** | ⚠️ **높음** | 스캔 대상 코인 즉시 변경 |
| **급등 감지 영향** | ⚠️ **높음** | 급등 추적 대상 즉시 변경 |
| **포지션 체크 영향** | ✅ **없음** | 보유 포지션 기준으로 독립 작동 |
| **개선 필요성** | ⚠️ **권장** | 스냅샷 + 추적 연속성 보장 |

---

## 📝 실행 주기 변경 요청 반영

### **사용자 요청**:
1. **전체 스캔**: 20초 → **60초**
2. **포지션 체크**: 7초 → **3초**
3. **급등 감지**: 5초 유지
4. **동적 갱신**: 3분 유지 + **영향 범위 분석 완료**

### **변경 후 효과**:
- ✅ **전체 스캔 간격 증가 (60초)**: 동적 갱신과 겹칠 확률 감소
- ✅ **포지션 체크 빠름 (3초)**: 청산 속도 향상
- ⚠️ **동적 갱신 영향**: 여전히 즉시 반영됨 (개선 필요)

---

## 🔧 다음 단계

1. **즉시 적용** (필수):
   - 전체 스캔: 20초 → 60초
   - 포지션 체크: 7초 → 3초

2. **추가 개선** (권장):
   - 스캔 시 스냅샷 사용 (중간 갱신 방지)
   - 급등 추적 연속성 보장 (점수 70+ 코인 보존)
   - OHLCV 캐싱 추가 (API 30% 절감)

3. **테스트**:
   - 30초 런타임 테스트
   - API 호출 수 재분석
   - 동적 갱신 시점 로그 확인

---

**작성자**: AI 개발 도우미  
**분석일**: 2026-02-13  
**다음 버전**: v6.30.9 (실행 주기 변경 + 동적 갱신 개선)
