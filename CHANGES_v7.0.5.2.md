# 🔧 v7.0.5.2 버그 수정 (Hotfix)

## 📅 배포 정보
- **버전**: v7.0.5.2
- **날짜**: 2026-07-27
- **유형**: 버그 수정 (Hotfix)
- **이전 버전**: v7.0.5.1

---

## 🐛 수정된 버그

### "현재가 조회 실패" 오류 완전 해결 ⭐

**문제점** (v7.0.5.1에서도 발생):
```
2026-07-27 22:00:05 [ERROR] ❌ 현재가 조회 실패
→ 빠른 스캔 중 API 제한 초과
→ 봇이 멈춤
```

**근본 원인**:
```python
# 빠른 스캔 시 API 호출 간격이 없음
for ticker in tickers[:10]:
    price = api.get_current_price(ticker)  # 즉시 호출
    # 0.1초도 안 걸리는 빠른 연속 호출
    # → Upbit 제한 (초당 10회) 초과
```

**해결책**:
```python
# v7.0.5.2: API 호출 간격 추가
for ticker in tickers[:10]:
    price = api.get_current_price(ticker)
    time.sleep(0.2)  # 0.2초 대기 (초당 5개)
    # → Upbit 제한 안전
```

---

## 🔧 주요 변경사항

### 1. API 호출 간격 추가

```python
# Before (v7.0.5.1):
for ticker in top_volume_tickers[:10]:
    current_price = self.api.get_current_price(ticker)
    if current_price and current_price > 0:
        candidates.append(ticker)
    # ❌ 간격 없음 → API 제한 초과

# After (v7.0.5.2):
for i, ticker in enumerate(top_volume_tickers[:10], 1):
    current_price = self.api.get_current_price(ticker)
    if current_price and current_price > 0:
        candidates.append(ticker)
    
    # ✅ 0.2초 대기 (초당 5개로 제한)
    time.sleep(0.2)
```

### 2. 로깅 강화

```python
# v7.0.5.2: 진행 상황 표시
logger.debug(f"   [{i}/10] {ticker}: {current_price:,.0f}원")
```

---

## 📊 개선 효과

### API 호출 속도

**Before (v7.0.5.1)**:
```
10개 코인 스캔: 0.5~1초
→ 초당 10~20회 호출
→ Upbit 제한 (10회/초) 초과 ❌
→ "현재가 조회 실패" 오류
```

**After (v7.0.5.2)**:
```
10개 코인 스캔: 2초
→ 초당 5회 호출
→ Upbit 제한 (10회/초) 안전 ✅
→ 오류 없음
```

### 스캔 시간

```
빠른 스캔 (10개): 2초
상세 스캔 (5개): 15초
총 스캔 시간: ~17초
스캔 간격: 30초

→ 충분한 여유 시간
```

---

## 🧪 테스트 결과

### 시뮬레이션 모드 (30초 테스트)

```
2026-07-27 13:09:28 [INFO] 🚀 Profit-Optimized Bot v7.0 시작 (모드: paper)
2026-07-27 13:09:28 [INFO] ✅ 초기화 완료!
2026-07-27 13:09:28 [INFO] 🔍 진입 기회 스캔 중...
2026-07-27 13:09:29 [INFO] ✅ 50개 코인 선정 (주요 18개 + 샘플 32개)
2026-07-27 13:09:29 [INFO] 📊 50개 코인 빠른 스캔 중...
2026-07-27 13:09:39 [INFO] 🎯 10개 후보 발견, 상세 분석 중...
2026-07-27 13:09:55 [INFO] ✅ 스캔 완료

✅ "현재가 조회 실패" 오류 없음
✅ 정상 동작
```

---

## 💡 기술적 배경

### Upbit API 제한

```
Rate Limit:
- REST API: 초당 10회
- 연속 요청: 제한 적용
- 초과 시: 1분간 차단

우리 봇:
- 빠른 스캔: 10개 코인 × 1회 = 10회
- 0.2초 간격 → 초당 5회로 안전하게 제한
- 추가 마진: 50% (10회 → 5회)
```

### 다른 봇들의 문제

```
대부분의 봇:
- 간격 없이 연속 호출
- API 제한 초과 발생
- 오류 처리 미흡

우리 봇 (v7.0.5.2):
- 0.2초 간격 강제
- API 제한 안전
- 안정적 동작 ✅
```

---

## 📝 업그레이드 가이드

### v7.0.5.1 → v7.0.5.2

**즉시 업그레이드 권장!**

**방법 1: 부분 교체 (빠름)**
1. `profit_bot_v7.py`만 다운로드
2. 기존 파일 교체
3. 즉시 사용 가능

**방법 2: 전체 교체**
1. 새 패키지 다운로드
2. 압축 해제 후 교체

**호환성**:
- ✅ 설정 파일 그대로
- ✅ 학습 데이터 그대로
- ✅ API 키 재설정 불필요

---

## 🔗 관련 버전

- **v7.0.5**: 호가창 유동성 체크 + 스마트 주문 추가
- **v7.0.5.1**: API 호출 최적화 (market_data 재사용)
- **v7.0.5.2**: API 호출 간격 추가 (완전 해결) ⭐ (현재)

---

## 🚨 중요 공지

**v7.0.5, v7.0.5.1 사용자는 즉시 v7.0.5.2로 업그레이드하세요!**

- v7.0.5: "현재가 조회 실패" 오류 ❌
- v7.0.5.1: 일부 개선, 여전히 오류 발생 ⚠️
- v7.0.5.2: 완전 해결 ✅

---

**작성일**: 2026-07-27  
**버전**: v7.0.5.2  
**유형**: Hotfix (Final)  
**상태**: 완료 ✅
